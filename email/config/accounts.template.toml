# 邮箱账户配置模板
# 复制此文件到项目目录并重命名为 email_config.toml（或自定义名称）
# 密码从环境变量读取：EMAIL_{ACCOUNT}_PASSWORD（account 大写）
# 环境变量在项目 .env 文件中配置
#
# 也可使用 email_tool.py init 交互式创建配置

[accounts.qqmail]
email = "your_email@example.com"
imap_host = "imap.qq.com"
imap_port = 993
smtp_host = "smtp.qq.com"
smtp_port = 465
